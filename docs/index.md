# ai-lit UI

基于 **Lit + TypeScript** 的 UI 组件库，使用 **Vite library mode** 打包并输出：

- ESM
- CJS
- UMD
- IIFE

## 开始使用

安装：

```bash
pnpm add @ai-lit/ui
```

使用（Web Components）：

```js
import "@ai-lit/ui";
```

然后在 HTML 中直接使用：

```html
<ai-button>Click</ai-button>
```

